import React from 'react';

export default function SpotsPage() {
  return (
    <section>
      <h1>Spots</h1>
      <p>Collection zones and drop-off spots that students can use for quick logging.</p>
      <p className="muted">Spots are stored in an in-memory database for this demo.</p>
    </section>
  );
}
