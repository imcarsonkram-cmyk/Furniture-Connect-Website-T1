// Placeholder server file recreated by assistant.
// The original server.js was deleted alongside the top-level `app/` folder.
// This file is intentionally minimal and harmless — replace with your original server code if you have it.

// If Next or your tooling expected this file, keeping a placeholder prevents "file not found" errors.

module.exports = function() {
  // no-op
  return {
    name: 'placeholder-server',
  };
};
